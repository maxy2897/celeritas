{
  "name": "celeritas-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && vite build --ssr worker.js",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "0.2.0",
    "vite": "^8.0.0"
  }
}
